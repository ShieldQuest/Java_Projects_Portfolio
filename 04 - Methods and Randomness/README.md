# Methods and Randomness

This folder contains programs demonstrating method design patterns and random number generation. The Payroll programs compare parameter-passing versus static global variables for sharing data between methods. The Random programs cover generating decimal values with Math.random() and simulating dice rolls by scaling random output to integer ranges.

These programs were developed during classroom instruction while following the professor's live coding examples on modular program design and randomness in Java.
