# Recursion

This folder introduces recursive programming through factorial calculations. One program computes factorial using a traditional iterative loop, while the other uses a recursive method that calls itself with progressively smaller values. Comparing the two approaches side by side highlights how recursion simplifies certain problems by breaking them into smaller subproblems.

Both programs were completed during guided classroom instruction while following the professor's demonstration on recursive thinking and base case design.
